<Window x:Class="VideoEditorUi.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewNavigator="clr-namespace:MVVMFramework.ViewNavigator;assembly=MVVMFramework"
        mc:Ignorable="d"
        Height="650" Width="800">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <viewNavigator:NavigationBar x:Name="navigationBar" Grid.Row="0" DataContext="{Binding Navigator}"/>
    <Separator Grid.Row="1" Height="50"/>
    <ContentControl Grid.Row="2" Content="{Binding Navigator.CurrentViewModel}"/>
  </Grid>
</Window>
